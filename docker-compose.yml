services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ../compendium-data/data.db:/app/var/data.db:Z
      - ../compendium-data/caddy:/data:Z
    env_file:
      - .env.prod